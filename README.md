

## Tech Stack

* React
* Vite
* Tailwind CSS
* React Router
* Axios

## Setup


Install dependencies:

```bash
npm install
```

Create a `.env` file:

```env
VITE_API_URL=http://localhost:4000/api
```

For the deployed version, use the deployed backend URL:

```env
VITE_API_URL=https://your-backend-url.onrender.com/api
```

## Run Locally

```bash
npm run dev
```

The frontend will run at:

```text
http://localhost:5173
```

## Build

```bash
npm run build
```

The production files will be generated in the `dist` folder.

## Features

* Login and authentication
* Role-based access
* Dashboard
* User management
* Task management
* Task assignment
* Task status updates
* Protected routes
* Responsive UI

